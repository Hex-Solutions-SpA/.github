name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────────────────────────────────
  # Replace the jobs below with the ones appropriate for this repo.
  # Each job name becomes a GitHub status check (format: "CI / <job-name>").
  # Add the checks you want required to the org ruleset in:
  # github.com/organizations/Hex-Solutions-SpA/settings/rules
  # ──────────────────────────────────────────────────────────────────────────

  # Example: Zephyr firmware repo
  # firmware-format:
  #   name: "clang-format"
  #   runs-on: [self-hosted, linux, zephyr, <repo-tag>]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: |
  #         FILES=$(find src include -type f \( -name '*.c' -o -name '*.h' \) 2>/dev/null || true)
  #         [ -z "$FILES" ] && exit 0
  #         clang-format -style=file -Werror --dry-run $FILES
  #
  # firmware-sca:
  #   name: "CodeChecker"
  #   needs: firmware-format
  #   runs-on: [self-hosted, linux, zephyr, <repo-tag>]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: |
  #         source .venv/bin/activate
  #         west build -b <board> --pristine -d build/sca . -- \
  #           -DZEPHYR_SCA_VARIANT=codechecker -DCODECHECKER_EXPORT=html \
  #           -DCODECHECKER_PARSE_EXIT_STATUS=1 \
  #           "-DCODECHECKER_ANALYZE_OPTS=--analyzers;clangsa;clang-tidy;--skip;${GITHUB_WORKSPACE}/.codechecker.skip;--analyzer-config=clang-tidy:cc-verbatim-args-file=${GITHUB_WORKSPACE}/.codechecker.tidyargs" \
  #           "-DCODECHECKER_PARSE_OPTS=--skip;${GITHUB_WORKSPACE}/.codechecker.skip"
  #
  # firmware-test:
  #   name: "west twister"
  #   needs: firmware-format
  #   runs-on: [self-hosted, linux, zephyr, <repo-tag>]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: |
  #         source .venv/bin/activate
  #         west twister -T tests -p native_sim -v --outdir twister-out

  placeholder:
    name: "Placeholder — replace this job"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replace this with real CI jobs for your repo."
